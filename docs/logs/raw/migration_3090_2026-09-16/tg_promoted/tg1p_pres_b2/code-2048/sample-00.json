{
 "started_utc": "2026-09-17T04:52:49.242570+00:00",
 "wall_seconds": 16.346331963999546,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:53:05.589081+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 59,
  "temperature_c": {
   "min": 73.0,
   "max": 76.0,
   "mean": 75.37288135593221
  },
  "power_w": {
   "min": 229.43,
   "max": 273.58,
   "mean": 256.49813559322035
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 91.2542372881356
  },
  "clock_sm_mhz": {
   "min": 1635.0,
   "max": 1905.0,
   "mean": 1886.1864406779662
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "88c262e75080248ab273a9eb496661619469efb64fb35fde5672333179254776",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] ->+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >>+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(t+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * stride_bsn\n+        b_scale_ptr += b_scale_src_off + b_scale_idx\n+        b_scale_ptr = tl.load(b_scale_ptr)\n+    elif use_int4_w4a16 or use_int2_w2a16:\n+        if has_zp:\n+            b_zp_src_off = off_experts * stride_bze\n+\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --\u5728\u5916\u5c42\u5faa\u73af\u5f15\u5165\u4e00\u4e2atry \u5757\uff0c\u51cf\u5c11\u5faa\u73af\u4e2d\u7684try\u6b21\u6570\n+        try:\n+            if use_int2_w2a16:\n+                # Load the words covering this K block: [BLOCK_SIZE_K//16, BLOCK_SIZE_N]\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                # unpack: [BLOCK_SIZE_K//16, 16, BLOCK_SIZE_N] -> [BLOCK_SIZE_K, BLOCK_SIZE_N]\n+                val = (val[:, None, :] >> b_shifter) & 0x3\n+                val = tl.reshape(val, (BLOCK_SIZE_K, BLOCK_SIZE_N))\n+            elif use_int4_w4a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = (val >> b_shifter) & 0xF\n+            elif use_int8_w8a16:\n+                val = tl.load(b_ptrs, mask=offs_k[:, None] < K - k - 1, other=0.0)\n+                val = val.to(tl.float32)\n+            else:\n+                val = tl.load(b_ptrs, mask",
 "ttft_seconds": 1.4813619019987527,
 "status": "ok",
 "tg_tokens_per_s": 34.37626449553335,
 "decode_seconds_per_token": 0.029089839011738297,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 28.987181998672895,
  "p95": 36.821814996073954,
  "mean": 29.0898390117383
 },
 "class": "code",
 "requested_input_tokens": 2048
}
