{
 "started_utc": "2026-09-17T04:50:53.935350+00:00",
 "wall_seconds": 16.90576310400502,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:51:10.841220+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 61,
  "temperature_c": {
   "min": 64.0,
   "max": 72.0,
   "mean": 69.32786885245902
  },
  "power_w": {
   "min": 127.49,
   "max": 268.98,
   "mean": 240.40983606557376
  },
  "util_pct": {
   "min": 0.0,
   "max": 96.0,
   "mean": 83.80327868852459
  },
  "clock_sm_mhz": {
   "min": 1740.0,
   "max": 1935.0,
   "mean": 1911.639344262295
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "adde61fa57c37ebd95ccd0bb51cb96fad931b52f202524fe29d82df59ad166aa",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,11.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34, ngram3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34, ngram35.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34, ngram35,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, ngram, nll, nll2, nll3, nll4, nll5, nll6, nll7, ngram2,\n                ngram3, ngram4, ngram5, ngram6, ngram7, ngram8, ngram9, ngram10, ngram11, ngram12,\n                ngram13, ngram14, ngram15, ngram16, ngram17, ngram18, ngram19, ngram20, ngram21,\n                ngram22, ngram23, ngram24, ngram25, ngram26, ngram27, ngram28, ngram29, ngram30,\n                ngram31, ngram32, ngram33, ngram34, ngram35, n",
 "ttft_seconds": 2.8207144159969175,
 "status": "ok",
 "tg_tokens_per_s": 36.279801166890564,
 "decode_seconds_per_token": 0.027563546872815096,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.481474007596262,
  "p95": 36.19684099976439,
  "mean": 27.563546872815095
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
