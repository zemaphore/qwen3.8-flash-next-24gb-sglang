{
 "started_utc": "2026-09-17T04:23:34.287551+00:00",
 "wall_seconds": 35.653618993994314,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:24:09.941241+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 128,
  "temperature_c": {
   "min": 72.0,
   "max": 77.0,
   "mean": 75.421875
  },
  "power_w": {
   "min": 164.5,
   "max": 279.87,
   "mean": 243.396875
  },
  "util_pct": {
   "min": 0.0,
   "max": 99.0,
   "mean": 81.765625
  },
  "clock_sm_mhz": {
   "min": 1395.0,
   "max": 1920.0,
   "mean": 1790.0390625
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1324.0
  }
 },
 "output_text_sha256": "dd55e580bf556d3939bd5ae8b28fb38dead9e86257026b6c5854a05049ac2c75",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the Mo+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (pref+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls +=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 ==+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._aut+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autof+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers.values()) <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers.values()) < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers.values()) < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers.values()) < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (or S = E).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <= reserve:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S_next = min(S + step, self.E)\n+            try:\n+                self.set_S(S_next)\n+            except ArenaOOM:\n+                self.set_S(S)\n+                break\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        free = torch.cuda.mem_get_info()[0]\n+        if free >= reserve:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S_next = max(S - step, self.s_floor())\n+            self.set_S(S_next)\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= reserve:\n+                break\n+            S = S_next\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, free / 2**30)\n+        self.pending_regrow = True\n+\n+    def poll(self):\n+        \"\"\"Called from the MoE apply path (prefill/eager only).\"\"\"\n+        self._calls += 1\n+        if self._calls % 100 == 0:\n+            self.write_status()\n+        if self._autofill is not None:\n+            free = torch.cuda.mem_get_info()[0]\n+            if free > (self._autofill << 20) and min(st.S for st in self.layers.values()) < self.E:\n",
 "ttft_seconds": 16.859445912996307,
 "status": "ok",
 "tg_tokens_per_s": 27.189368314243424,
 "decode_seconds_per_token": 0.036779081751455764,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 37.317921000067145,
  "p95": 45.910904998891056,
  "mean": 36.77908175145576
 },
 "class": "code",
 "requested_input_tokens": 32768
}
