{
 "started_utc": "2026-09-17T04:24:09.944113+00:00",
 "wall_seconds": 20.46764100099972,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:24:30.411893+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 73,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 75.68493150684931
  },
  "power_w": {
   "min": 234.02,
   "max": 277.07,
   "mean": 243.01547945205482
  },
  "util_pct": {
   "min": 89.0,
   "max": 99.0,
   "mean": 92.4931506849315
  },
  "clock_sm_mhz": {
   "min": 1455.0,
   "max": 1920.0,
   "mean": 1898.835616438356
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2084.0
  }
 },
 "output_text_sha256": "fcea9e215870f7e100b9bfa7ef8dbc57c2ec737b49ce5818bf167eb0d3e810d7",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-me+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free <= reserve_mb:\n+                break\n+            room = int((free - reserve_mb) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = min(self.E, S + max(step, min(step, room)))\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S = min(st.S for st in self.layers.values())\n+                break\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] / 2**20\n+            if free >= reserve_mb:\n+                break\n+            need = int((reserve_mb - free) * 2**20 / max(1, self.L * sum(k.rb for k in next(iter(self.layers.values()).kinds.values()))))\n+            S = max(self.s_floor(), S - max(step, min(step, need)))\n+            self.set_S(S)\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {min(st.S for st in self.layers.values())} \"\n+                 f\"free {free / 2**20:.0f}MB total {total / 2**20:.0f}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds",
 "ttft_seconds": 1.349874835999799,
 "status": "ok",
 "tg_tokens_per_s": 26.72916854471986,
 "decode_seconds_per_token": 0.03741231225830787,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 37.88735199486837,
  "p95": 45.700675997068174,
  "mean": 37.41231225830787
 },
 "class": "code",
 "requested_input_tokens": 32768
}
