{
 "started_utc": "2026-09-17T04:25:08.798931+00:00",
 "wall_seconds": 18.86683355900459,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:25:27.665837+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 68,
  "temperature_c": {
   "min": 75.0,
   "max": 76.0,
   "mean": 75.8529411764706
  },
  "power_w": {
   "min": 231.13,
   "max": 257.53,
   "mean": 243.00441176470588
  },
  "util_pct": {
   "min": 88.0,
   "max": 96.0,
   "mean": 93.19117647058823
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1919.1176470588234
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2080.0
  }
 },
 "output_text_sha256": "4f1619c5e12efb4232ecbdda312b19323dd1e5bec0bb2898f590d5e60ec79d19",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                        +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum(len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum(len(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum(len(self.pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum(len(self.pool[k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum(len(self.pool[k.name+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum(len(self.pool[k.name])+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] <= reserve:\n+                break\n+            S = min(self.E, self.S0 + step)\n+            if S <= self.S0:\n+                break\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                break\n+            self.S0 = S\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        reserve = int(reserve_mb) << 20\n+        for _ in range(self.E // step):\n+            if torch.cuda.mem_get_info()[0] >= reserve:\n+                break\n+            S = max(self.s_floor(), self.S0 - step)\n+            if S >= self.S0:\n+                break\n+            self.set_S(S)\n+            self.S0 = S\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", self.S0, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"epoch {int(time.time())} S {self.S0} free {free // 2**20} total {total // 2**20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            lines.append(f\"L {lid} S {st.S} arena {sum(k.arena.bytes for k in st.kinds.values()) // 2**20} \"\n+                         f\"pool {sum(len(self.pool[k.name]) for",
 "ttft_seconds": 0.411985396000091,
 "status": "ok",
 "tg_tokens_per_s": 27.689283183957297,
 "decode_seconds_per_token": 0.036115055538143474,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 36.558825988322496,
  "p95": 44.219944000360556,
  "mean": 36.11505553814347
 },
 "class": "code",
 "requested_input_tokens": 32768
}
