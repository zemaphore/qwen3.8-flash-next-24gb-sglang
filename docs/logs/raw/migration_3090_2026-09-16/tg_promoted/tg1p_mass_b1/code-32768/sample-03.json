{
 "started_utc": "2026-09-17T04:24:49.750151+00:00",
 "wall_seconds": 19.04605667499709,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:25:08.796276+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 68,
  "temperature_c": {
   "min": 75.0,
   "max": 76.0,
   "mean": 75.79411764705883
  },
  "power_w": {
   "min": 233.24,
   "max": 253.51,
   "mean": 242.36970588235295
  },
  "util_pct": {
   "min": 78.0,
   "max": 96.0,
   "mean": 92.67647058823529
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1919.5588235294117
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2080.0
  }
 },
 "output_text_sha256": "2152590775b52fa56048d456110867893cf9d24bc9bfb8ae90b28ae098feca98",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-me+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={arena.n_rows * arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={arena.n_rows * arena.row+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                self.set_S(S - step)\n+                break\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic fill(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0] >> 20\n+        if free >= reserve_mb:\n+            return\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        logger.info(\"elastic free(%d MB): S=%d, VRAM free %.2f GB\", reserve_mb, S, free / 2**30)\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"L{lid}: S={st.S} arena={arena.n_rows * arena.row_bytes",
 "ttft_seconds": 0.41293138499895576,
 "status": "ok",
 "tg_tokens_per_s": 27.424348588840818,
 "decode_seconds_per_token": 0.03646394723872887,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 36.7667190002976,
  "p95": 45.02748299273662,
  "mean": 36.46394723872886
 },
 "class": "code",
 "requested_input_tokens": 32768
}
