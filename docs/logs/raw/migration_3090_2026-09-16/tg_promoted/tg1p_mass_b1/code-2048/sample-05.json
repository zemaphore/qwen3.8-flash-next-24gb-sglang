{
 "started_utc": "2026-09-17T04:23:16.227701+00:00",
 "wall_seconds": 18.05454519300838,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:23:34.282463+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 65,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 76.07692307692308
  },
  "power_w": {
   "min": 233.47,
   "max": 261.56,
   "mean": 246.8386153846154
  },
  "util_pct": {
   "min": 90.0,
   "max": 96.0,
   "mean": 92.78461538461538
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "2d1e7837d08242359900bdfe196e7de89fe1f8b973e918c529a050929c252ace",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # ------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits ==+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits == +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits == 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits == 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits == 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits == 4\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits == 4\n+        ):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = (offs_bn[None, :] % 2) * 8\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a k iteration sequence\n+        # and iterate this block with the list of expert_ids.\n+        if even_Ks:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=token_mask,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs)\n+        else:\n+            a = tl.load(\n+                a_ptrs,\n+                mask=offs_k[:, None] < K - k * BLOCK_SIZE_K,\n+                other=0.0,\n+            )\n+            b = tl.load(b_ptrs, mask=offs_k[:, None] < K - k * BLOCK_SIZE_K, other=0)\n+\n+        value_bits = (\n+            use_int2_w2a16\n+            or use_int4_w4a16\n+            or use_int8_w8a16\n+        )\n+\n+        if (use_int4_w4a16 or use_int8_w8a16) and (\n+            not has_zp and value_bits == 4\n+        ):\n",
 "ttft_seconds": 0.37849664001259953,
 "status": "ok",
 "tg_tokens_per_s": 28.909372377973998,
 "decode_seconds_per_token": 0.03459085818002394,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 34.75931700086221,
  "p95": 44.95243399287574,
  "mean": 34.59085818002394
 },
 "class": "code",
 "requested_input_tokens": 2048
}
