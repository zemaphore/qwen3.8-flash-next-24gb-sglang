{
 "started_utc": "2026-09-17T04:19:44.818171+00:00",
 "wall_seconds": 13.465079246001551,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:19:58.283419+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 48,
  "temperature_c": {
   "min": 74.0,
   "max": 75.0,
   "mean": 74.83333333333333
  },
  "power_w": {
   "min": 246.24,
   "max": 274.26,
   "mean": 266.88729166666667
  },
  "util_pct": {
   "min": 86.0,
   "max": 93.0,
   "mean": 90.0625
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1915.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "963fea61ed5e2dd0d8dc26aa0b0b223808bd2c6a7efb79ea492aa31b95f354f5",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, N.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/C.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md  .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DES.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md    .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md    .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/   .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE table).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE table)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE table)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE table)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE table)\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file). The server's own\n`--max-prompt-tokens` is 262,144; `--max-running-requests` is the concurrency knob.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files, applied in order)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV pool, PLE gather, PLE n-gram lookup)\nassets/         the two serving assets: the PLE n-gram table and the int2 checkpoint\ntools/          the measurement tools (bench, PLE probe, KV-tier probe, NLL probe, decode probe,\n                expert-cache controller, expert-cache probe, PLE table builder, PLE table validator)\nscripts/        the launcher and the measurement scripts\ndocs/           the design documents, the engineering log, the measurement logs\ndocs/CAMPAIGN.md   the append-only engineering log (dated entries, one per line)\ndocs/DESIGN.md     the design document (the architecture and the decisions)\ndocs/README.md     the document index (what to read, in order)\ndocs/logs/       the measurement logs (the raw output of the tools)\ndocs/figures/    the figures (the decode curve, the expert-cache floor, the PLE table)\ndocs/notes/",
 "ttft_seconds": 0.3030972809938248,
 "status": "ok",
 "tg_tokens_per_s": 38.824158235586616,
 "decode_seconds_per_token": 0.025757158569464873,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.089027010835707,
  "p95": 32.932004003669135,
  "mean": 25.757158569464874
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
