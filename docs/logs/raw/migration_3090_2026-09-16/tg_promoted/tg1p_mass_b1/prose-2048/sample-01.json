{
 "started_utc": "2026-09-17T04:19:01.750076+00:00",
 "wall_seconds": 15.095876310995664,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:19:16.846213+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 54,
  "temperature_c": {
   "min": 72.0,
   "max": 73.0,
   "mean": 72.5925925925926
  },
  "power_w": {
   "min": 253.73,
   "max": 270.67,
   "mean": 262.5611111111111
  },
  "util_pct": {
   "min": 84.0,
   "max": 98.0,
   "mean": 89.61111111111111
  },
  "clock_sm_mhz": {
   "min": 1800.0,
   "max": 1935.0,
   "mean": 1912.7777777777778
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "ace327ebf86aae84a406770be0dfe123cdeb61d4a42baaafd7705f24eb8398a3",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-cap.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,38.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,38.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/    .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,05.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram table),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram table), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram table), `m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram table), `moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram table), `moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\nThe PLE table is read from `PLE` (default `~/quant/model/ple`); `--ple-emb-table-path` is the directory, not\nthe `.bin` file. The server's `--ple-capacity` is 16,384 tokens, which is the capacity the table was built\nfor; the table's own capacity is 16,384 tokens (CAMPAIGN.md:448).\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from\nkernels/       the Triton kernels, with the tuned configs for sm_120\nassets/        the two serving assets: the PLE table and the expert cache\ntools/         the measurement tools\ndocs/          the design documents and the engineering log\ndocs/logs/     the dated logs of the runs\nscripts/       the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at `73a255206f`\nand served the model. It is 1,050 lines over 10 files: `qwen4exp.py` (the model), `qwen4exp_moe.py` (the\nMoE path), `ple.py` (the PLE), `moe.py` (the routing), `moe_ngram.py` (the n-gram table), `moe.py`",
 "ttft_seconds": 1.3413438829884399,
 "status": "ok",
 "tg_tokens_per_s": 37.15169216486132,
 "decode_seconds_per_token": 0.026916674362031247,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.618809992214665,
  "p95": 34.702482997090556,
  "mean": 26.916674362031248
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
