{
 "started_utc": "2026-09-17T04:19:31.383494+00:00",
 "wall_seconds": 13.43290156799776,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:19:44.816464+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 48,
  "temperature_c": {
   "min": 73.0,
   "max": 75.0,
   "mean": 74.45833333333333
  },
  "power_w": {
   "min": 245.27,
   "max": 276.69,
   "mean": 267.19458333333336
  },
  "util_pct": {
   "min": 85.0,
   "max": 92.0,
   "mean": 89.75
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1916.875
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "d47b7dc5332487f549155acaea6186b41141c5c86c362908344d9e29daf45e6d",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `k.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,05.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_tier.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_tier.py`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_tier.py`, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_tier.py`, `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_tier.py`, `ple_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch against SGLang `73a255206f` (the only patch that is applied at launch) |\n| `patches/` | the layered patch scripts the serving patch was built from, plus the serving assets |\n| `kernels/` | the Triton kernels, with the sm_120 configs and the kernel tests |\n| `tools/` | the measurement tools |\n| `scripts/` | the launcher and the measurement scripts |\n| `docs/` | the design documents, the engineering log and the measurement logs |\n| `assets/` | the accepted state and the measurement artifacts |\n| `tests/` | the repository's own tests |\n| `requirements/` | the pinned Python environment |\n\n`sglang/` holds the serving patch, `qwen4exp-serving-73a255206f.patch` (1,050 lines, 13 files), and\n`README.md`, which describes the patch and the serving code it adds. `patches/` holds the 13 layered patch\nscripts the serving patch was built from (`build_*.py`), the serving assets (`ple.py`, `ple_table.py`,\n`ple_tier.py`, `ple_tier.py`, `kv_tiers.py`, `ple_tier.py`, `ple_tier",
 "ttft_seconds": 0.3140189109981293,
 "status": "ok",
 "tg_tokens_per_s": 38.95163686630159,
 "decode_seconds_per_token": 0.025672862052817467,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 24.935531997471116,
  "p95": 33.52099499898031,
  "mean": 25.672862052817468
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
