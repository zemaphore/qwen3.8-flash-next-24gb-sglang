{
 "started_utc": "2026-09-17T04:18:44.554632+00:00",
 "wall_seconds": 17.1931036439928,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:19:01.747839+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 62,
  "temperature_c": {
   "min": 63.0,
   "max": 72.0,
   "mean": 69.91935483870968
  },
  "power_w": {
   "min": 117.04,
   "max": 265.48,
   "mean": 241.40483870967742
  },
  "util_pct": {
   "min": 0.0,
   "max": 96.0,
   "mean": 78.33870967741936
  },
  "clock_sm_mhz": {
   "min": 1830.0,
   "max": 1950.0,
   "mean": 1930.4032258064517
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "f9ecdc7de5089963383d6f0ee58dc17fc7bbbdd6325117b47aef35bf7dd177ce",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of Flash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash34.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash34,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash34, hash.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash34, hash3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash34, hash35.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the launcher derives `ple.json` from it).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the PLE, the tiered KV cache, the elastic mode,\n              the expert cache, the Triton configs, the sm_120 JIT configs, the CUDA 13 build, the sm_120 build\n              of FlashAttention 2, the sm_120a nvcc fix, the CUDA 13 compiler install, the sm_120 Triton configs\nkernels/        the Triton kernels: the int2 mixed MoE path, the int4 KV pool, the int8 ring, the PLE\n              (gather, hash, hash2, hash3, hash4, hash5, hash6, hash7, hash8, hash9, hash10, hash11, hash12,\n              hash13, hash14, hash15, hash16, hash17, hash18, hash19, hash20, hash21, hash22, hash23, hash24,\n              hash25, hash26, hash27, hash28, hash29, hash30, hash31, hash32, hash33, hash34, hash35,",
 "ttft_seconds": 2.894384154002182,
 "status": "ok",
 "tg_tokens_per_s": 35.737668547640595,
 "decode_seconds_per_token": 0.027981679853204083,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.07185699546244,
  "p95": 37.70057199290022,
  "mean": 27.98167985320408
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
