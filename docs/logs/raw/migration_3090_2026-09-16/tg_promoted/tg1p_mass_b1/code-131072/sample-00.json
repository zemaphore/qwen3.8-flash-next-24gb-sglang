{
 "started_utc": "2026-09-17T04:32:54.562772+00:00",
 "wall_seconds": 101.07323745600297,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:34:35.636147+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 362,
  "temperature_c": {
   "min": 71.0,
   "max": 76.0,
   "mean": 74.23204419889503
  },
  "power_w": {
   "min": 143.28,
   "max": 283.08,
   "mean": 231.61588397790055
  },
  "util_pct": {
   "min": 0.0,
   "max": 100.0,
   "mean": 65.16022099447514
  },
  "clock_sm_mhz": {
   "min": 1290.0,
   "max": 1920.0,
   "mean": 1712.9005524861877
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 108.0
  }
 },
 "output_text_sha256": "69f04deef2ed9a3fa6b4e51ada3569fdc0386e37f302bdefe9ae174a148c66af",
 "output_text": "\"\"   \"    LOG\"    LOGPRO\"    LOGPROB\"    LOGPROB_MAX\"    LOGPROB_MAX={\"    LOGPROB_MAX={overall\"    LOGPROB_MAX={overall['\"    LOGPROB_MAX={overall['max\"    LOGPROB_MAX={overall['max']\"    LOGPROB_MAX={overall['max']:.\"    LOGPROB_MAX={overall['max']:.6\"    LOGPROB_MAX={overall['max']:.6f\"    LOGPROB_MAX={overall['max']:.6f}\"    LOGPROB_MAX={overall['max']:.6f} LOG\"    LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() ->\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-t\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(col\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(),\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) +\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.t\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'E\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUI\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVA\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALE\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT'\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else '\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUI\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVA\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALE\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong Mo\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontin\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference. \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logpro\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude;\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.00\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 logprob_diff\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 logprob_diff.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 logprob_diff.py save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 logprob_diff.py save NAME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 logprob_diff.py save NAME\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--tol\", type=float, default=0.01)\n    parser.add_argument(\"--max-tol\", type=float, default=0.5)\n    args = parser.parse_args()\n\n    if args.mode == \"save\":\n        name = args.names[0]\n        path = D / f\"{name}.json\"\n        path.write_text(json.dumps(collect(), indent=2) + \"\\n\")\n        print(f\"  saved {path}\")\n        return\n\n    if args.mode == \"compare\":\n        a_name, b_name = args.names\n        a = json.loads((D / f\"{a_name}.json\").read_text())\n        b = json.loads((D / f\"{b_name}.json\").read_text())\n        print_report(f\"{b_name} vs {a_name}\", stats(a, b))\n        print_report(f\"{a_name} vs {b_name}\", stats(b, a))\n        return\n\n    name = args.names[0]\n    reference = json.loads((D / f\"{name}.json\").read_text())\n    current = collect(reference)\n    report = stats(reference, current)\n    print_report(f\"{name} vs current\", report)\n    overall = report[\"_overall\"]\n    ok = overall[\"mean\"] <= args.tol and overall[\"max\"] <= args.max_tol\n    print(f\"  {'EQUIVALENT' if ok else 'NOT EQUIVALENT'}\")\n    sys.exit(0 if ok else 1)\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle.\n\nThe only correctness check that catches a wrong MoE config: it scores a fixed\ncontinuation after a fixed prompt and reports the maximum absolute logprob\ndifference against a reference.  A wrong kernel or config moves the logprobs by\norders of magnitude; the same configuration run twice differs by ~0.002 nats.\n\n  python3 logprob_diff.py save NAME\n ",
 "ttft_seconds": 82.3831150180049,
 "status": "ok",
 "tg_tokens_per_s": 27.34074590775208,
 "decode_seconds_per_token": 0.036575446894316965,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 37.260005992720835,
  "p95": 46.08025000197813,
  "mean": 36.57544689431697
 },
 "class": "code",
 "requested_input_tokens": 131072
}
