{
 "started_utc": "2026-09-17T04:35:14.366345+00:00",
 "wall_seconds": 19.879864157002885,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:35:34.246314+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 71,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 75.97183098591549
  },
  "power_w": {
   "min": 221.72,
   "max": 250.57,
   "mean": 240.7705633802817
  },
  "util_pct": {
   "min": 90.0,
   "max": 95.0,
   "mean": 93.40845070422536
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 108.0
  }
 },
 "output_text_sha256": "72585c19e19b19f3de9c8bb2a3dac56bc102ce5805ead5959ac45eb1fc0ed1ba",
 "output_text": "\"\"   \"    LOG\"    LOGPRO\"    LOGPROB\"    LOGPROB_MAX\"    LOGPROB_MAX={\"    LOGPROB_MAX={overall\"    LOGPROB_MAX={overall['\"    LOGPROB_MAX={overall['max\"    LOGPROB_MAX={overall['max']\"    LOGPROB_MAX={overall['max']:.\"    LOGPROB_MAX={overall['max']:.6\"    LOGPROB_MAX={overall['max']:.6f\"    LOGPROB_MAX={overall['max']:.6f}\"    LOGPROB_MAX={overall['max']:.6f} LOG\"    LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() ->\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parent\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logpro\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logpro\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0],\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) >\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or '\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) !=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise System\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a, b))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a, b))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a, b))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a, b))\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"mode\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--dir\", type=Path, default=D)\n    args = parser.parse_args()\n\n    args.dir.mkdir(parents=True, exist_ok=True)\n    if args.mode == \"save\":\n        name = args.names[0]\n        payload = collect()\n        (args.dir / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {args.dir / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_prompt_tokens']:6d} \"\n                f\"forced={row['forced_prompt_tokens']:6d} \"\n                f\"logprobs={len(row['logprobs'])}\"\n            )\n        return\n\n    if args.mode == \"check\":\n        ref_name, cur_name = args.names[0], args.names[1] if len(args.names) > 1 else None\n        reference = json.loads((args.dir / f\"{ref_name}.json\").read_text())\n        if cur_name is None:\n            current = collect(reference)\n        else:\n            current = json.loads((args.dir / f\"{cur_name}.json\").read_text())\n        print_report(f\"{ref_name} vs {cur_name or 'live server'}\", stats(reference, current))\n        return\n\n    if args.mode == \"compare\":\n        if len(args.names) != 2:\n            raise SystemExit(\"compare needs exactly two saved names\")\n        a = json.loads((args.dir / f\"{args.names[0]}.json\").read_text())\n        b = json.loads((args.dir / f\"{args.names[1]}.json\").read_text())\n        print_report(f\"{args.names[1]} vs {args.names[0]}\", stats(b, a))\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", stats(a, b))\n        return\n\n\n",
 "ttft_seconds": 0.5101675850019092,
 "status": "ok",
 "tg_tokens_per_s": 26.38150055002344,
 "decode_seconds_per_token": 0.03790534954991829,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 37.686941999709234,
  "p95": 46.97839501022827,
  "mean": 37.90534954991829
 },
 "class": "code",
 "requested_input_tokens": 131072
}
