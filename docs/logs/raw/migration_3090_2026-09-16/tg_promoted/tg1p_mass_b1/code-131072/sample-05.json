{
 "started_utc": "2026-09-17T04:35:52.947229+00:00",
 "wall_seconds": 19.377196715999162,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:36:12.324603+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 69,
  "temperature_c": {
   "min": 75.0,
   "max": 78.0,
   "mean": 76.14492753623189
  },
  "power_w": {
   "min": 228.8,
   "max": 270.11,
   "mean": 242.29898550724639
  },
  "util_pct": {
   "min": 88.0,
   "max": 96.0,
   "mean": 93.07246376811594
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 108.0
  }
 },
 "output_text_sha256": "378b861a2773b4b5adce17c48ba93e06c7e220dde3aca85cf5c9b5e789d27e24",
 "output_text": "\"\"   \"    LOG\"    LOGPRO\"    LOGPROB\"    LOGPROB_MAX\"    LOGPROB_MAX={\"    LOGPROB_MAX={overall\"    LOGPROB_MAX={overall['\"    LOGPROB_MAX={overall['max\"    LOGPROB_MAX={overall['max']\"    LOGPROB_MAX={overall['max']:.\"    LOGPROB_MAX={overall['max']:.6\"    LOGPROB_MAX={overall['max']:.6f\"    LOGPROB_MAX={overall['max']:.6f}\"    LOGPROB_MAX={overall['max']:.6f} LOG\"    LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() ->\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] !=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} !=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n               \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(c\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0],\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME     \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      #\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (gre\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME    \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     #\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare A\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare A B\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare A B   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare A B    #\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare A B    # compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare A B    # compare two\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    parser = argparse.ArgumentParser(description=__doc__)\n    parser.add_argument(\"cmd\", choices=(\"save\", \"check\", \"compare\"))\n    parser.add_argument(\"names\", nargs=\"+\")\n    parser.add_argument(\"--no-server-check\", action=\"store_true\")\n    args = parser.parse_args()\n\n    if args.cmd == \"save\":\n        name = args.names[0]\n        payload = collect()\n        if not args.no_server_check:\n            for key, row in payload.items():\n                if row[\"prompt_tokens\"] != row[\"server_tokens\"]:\n                    raise RuntimeError(\n                        f\"{key}: local {row['prompt_tokens']} != server {row['server_tokens']}\"\n                    )\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        print(f\"  saved {D / name}.json\")\n        for key, row in payload.items():\n            print(\n                f\"    {key:14s} prompt={row['prompt_tokens']:6d} \"\n                f\"server={row['server_tokens']:6d} forced={row['forced_tokens']:3d}\"\n            )\n        return\n\n    if args.cmd == \"check\":\n        reference = json.loads((D / args.names[0]).read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(args.names[0], report)\n        return\n\n    if args.cmd == \"compare\":\n        a = json.loads((D / args.names[0]).read_text())\n        b = json.loads((D / args.names[0]).read_text())\n        report = stats(a, b)\n        print_report(f\"{args.names[0]} vs {args.names[1]}\", report)\n        return\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Teacher-forced logprob oracle: greedy decode vs a reference capture.\n\n  python3 logprob_diff.py save NAME      # capture the reference (greedy decode, forced tokens)\n  python3 logprob_diff.py check NAME     # decode now and compare against NAME\n  python3 logprob_diff.py compare A B    # compare two saved",
 "ttft_seconds": 0.4731787630007602,
 "status": "ok",
 "tg_tokens_per_s": 27.03139339841148,
 "decode_seconds_per_token": 0.03699402340312822,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 37.872520988457836,
  "p95": 46.729273002711125,
  "mean": 36.99402340312822
 },
 "class": "code",
 "requested_input_tokens": 131072
}
