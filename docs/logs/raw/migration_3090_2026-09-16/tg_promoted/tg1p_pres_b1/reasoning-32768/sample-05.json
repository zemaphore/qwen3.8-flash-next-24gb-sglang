{
 "started_utc": "2026-09-17T04:04:54.368953+00:00",
 "wall_seconds": 14.310549832007382,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:05:08.679647+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 51,
  "temperature_c": {
   "min": 77.0,
   "max": 78.0,
   "mean": 77.54901960784314
  },
  "power_w": {
   "min": 256.37,
   "max": 273.41,
   "mean": 264.3141176470588
  },
  "util_pct": {
   "min": 81.0,
   "max": 92.0,
   "mean": 88.31372549019608
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1915.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1092.0
  }
 },
 "output_text_sha256": "bd3c7fd48d0c8d4357993e269796c971a6789d8eb5cb831c11e7266138b2f85b",
 "output_text": " last last  last 1 last 16 last 16 tokens last 16 tokens of last 16 tokens of every last 16 tokens of every request last 16 tokens of every request, last 16 tokens of every request, so last 16 tokens of every request, so f last 16 tokens of every request, so f >= last 16 tokens of every request, so f >=  last 16 tokens of every request, so f >= 0 last 16 tokens of every request, so f >= 0. last 16 tokens of every request, so f >= 0.0 last 16 tokens of every request, so f >= 0.06 last 16 tokens of every request, so f >= 0.06.\", last 16 tokens of every request, so f >= 0.06.\",\n last 16 tokens of every request, so f >= 0.06.\",\n  last 16 tokens of every request, so f >= 0.06.\",\n  \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/s last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (me last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step =  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2* last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sig last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-qu last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quart last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (me last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Prop last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: log last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)* last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (log last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_c last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is un last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unme last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) >  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta >  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~9 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f= last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a pre last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v). The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v). The int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v). The int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v). The int4 tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v). The int4 tier is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot fp8 unit scale = 2.64% relative RMS (measured on N(0,3)); cold int4-g32 = 8.6% relative RMS (step = 2*sigma/15). K smoothing from the k_norm weight makes rows unit-RMS with near-equal channel sigmas (max/median 1.00-1.04), so int4-g32 K error is ~8.6% per element; V is not smoothed -> cold V error 8.6% on average but 15-20% on the bottom-quartile V channels (measured channel sigma spread 1.2-2.5x). Propagation: logit noise std = (1/16)*sqrt(sum_j q_j^2 e_j^2) with q from the indexer's own bf16 keys (exact); softmax weight noise ~1.5% RMS (logit std ~2.4 nats); attention output error = sum_i p_i * err_i, bounded by err_cold * R(T) where R(T) = fraction of attention mass on cold tokens. R(T) is the design's only accuracy variable and is unmeasured: the indexer selects by content, so cold tokens are revived when relevant; the design's own kill rule is R(T) > 0.05 or NLL delta > 0.005 nats. Worst case: a request whose whole history is cold (a long prompt whose selection is diffuse) gets 8.6% K/V noise on ~95% of keys -> output error ~8-10% RMS, which is ~3x the fp8 error and would show as a measurable NLL delta. Best case (f=0.25, selection concentrated on recent tokens): R(T) ~0.1 -> output error ~1% RMS, NLL delta ~0.001-0.003 nats. The newest tokens are always exact fp8 (write tier = hot, ring of the last 16 tokens per request is hot), and the first chunk of a prefill is exact (no-prefix path reads the projected bf16 k/v). The int4 tier is the",
 "ttft_seconds": 0.3237642780004535,
 "status": "ok",
 "tg_tokens_per_s": 36.53466073012616,
 "decode_seconds_per_token": 0.027371268270062483,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.585925006656907,
  "p95": 35.10621900204569,
  "mean": 27.371268270062483
 },
 "class": "reasoning",
 "requested_input_tokens": 32768
}
