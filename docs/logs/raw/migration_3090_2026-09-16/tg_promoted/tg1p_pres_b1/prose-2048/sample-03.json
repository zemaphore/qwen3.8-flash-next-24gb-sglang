{
 "started_utc": "2026-09-17T03:55:45.093719+00:00",
 "wall_seconds": 14.045098024012987,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:55:59.138893+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 51,
  "temperature_c": {
   "min": 70.0,
   "max": 72.0,
   "mean": 70.94117647058823
  },
  "power_w": {
   "min": 251.37,
   "max": 268.89,
   "mean": 261.59352941176473
  },
  "util_pct": {
   "min": 85.0,
   "max": 93.0,
   "mean": 88.90196078431373
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1935.0,
   "mean": 1934.4117647058824
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "87f4e711d8403f31dcc276ca4b78efe626b03fec255b45bd5821f1cf3fb5cb4c",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:44.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/    .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,01.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:15.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :16.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :16.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :17.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :17.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :18.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :18.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :185.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :185,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :185, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :185, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :185, :19.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context (CAMPAIGN.md:448).\n`--enable-ple` alone is not enough: `scripts/serve.sh` also applies the `ple` patch, which is what makes the\nPLE path run at all. The `--ple-ngram-table-path` argument is `ple/ple.json` (the manifest), not the `.bin`\nfile; `ple/ple.json` names `ple.f8_e4m3.bin` next to it.\n\n## Repository layout\n\n```\nsglang/        the serving patch (one file)\npatches/       the layered patch scripts the patch was built from (10 files)\nkernels/       the Triton kernels (4 files)\nassets/        the two serving assets (2 files)\ntools/         the measurement tools (5 files)\ndocs/          the design documents and the engineering log (10 files)\ndocs/logs/     the dated logs (10 files)\nscripts/       the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured: 10 files, 1,010 added lines, 11 removed. It is the final state of the layered\npatch scripts in `patches/` (CAMPAIGN.md:155, :160, :164, :170, :175, :180, :185, :190",
 "ttft_seconds": 0.27033097700041253,
 "status": "ok",
 "tg_tokens_per_s": 37.09695736975682,
 "decode_seconds_per_token": 0.026956388634051344,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.506356996833347,
  "p95": 33.31045000231825,
  "mean": 26.956388634051343
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
