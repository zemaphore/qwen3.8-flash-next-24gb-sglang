{
 "started_utc": "2026-09-17T03:56:13.001481+00:00",
 "wall_seconds": 13.63241667499824,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:56:26.633998+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 49,
  "temperature_c": {
   "min": 71.0,
   "max": 73.0,
   "mean": 72.6938775510204
  },
  "power_w": {
   "min": 251.77,
   "max": 272.74,
   "mean": 265.3614285714286
  },
  "util_pct": {
   "min": 88.0,
   "max": 93.0,
   "mean": 90.51020408163265
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1935.0,
   "mean": 1932.857142857143
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "7718eccad749d595468d28499303322a87a469ad844616ed3a773c6a097dfb23",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,27.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s pre.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-cap.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,04.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,57.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,\n              ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,\n              ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (CAMPAIGN.md:413);\n`--max-running-requests 2` was measured at 54.5 tok/s decode and 2,271 tok/s prefill (CAMPAIGN.md:413).\n`--enable-ple` is required: the PLE n-gram table is a 51 GB mmap on the host, so the host RAM requirement is\n32 GB. The `--ple-emb-table-path` argument is `ple/ple.json` (the table's metadata; the `.bin` is the data);\n`--ple-ngram-capacity` must match the table's `ngram_capacity` (1,048,576).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch's manifest, the KV-tier and elastic-control modules\nkernels/        the Triton kernels: int2 MoE gemm, int8 gemm, int4 KV page copy, PLE gather\nscripts/        serve.sh (the launcher), the build and validation scripts\ntools/          the measurement tools: bench.py, ngram_ple.py, ngram_ple_test.py, ngram_ple_validate.py,\n              ngram_ple_validate_test.py, ngram_ple_validate_test.sh, ngram_ple_validate_test.sh.log,\n              ngram_ple_validate_test.sh.log.log, ngram_ple_validate_test.sh.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log, ngram_ple_validate_test.sh.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log,\n              ngram_ple_validate_test.sh.log.log.log.log.log.log.log,\n              ngram_ple_validate",
 "ttft_seconds": 0.27504055500321556,
 "status": "ok",
 "tg_tokens_per_s": 38.256255332085225,
 "decode_seconds_per_token": 0.026139516043048462,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.86565299134236,
  "p95": 31.228225998347625,
  "mean": 26.139516043048463
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
