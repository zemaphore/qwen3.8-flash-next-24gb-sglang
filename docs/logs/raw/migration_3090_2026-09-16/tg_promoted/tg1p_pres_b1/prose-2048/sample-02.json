{
 "started_utc": "2026-09-17T03:55:30.897542+00:00",
 "wall_seconds": 14.194357115004095,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:55:45.091973+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 51,
  "temperature_c": {
   "min": 67.0,
   "max": 71.0,
   "mean": 69.84313725490196
  },
  "power_w": {
   "min": 227.0,
   "max": 269.03,
   "mean": 257.5296078431373
  },
  "util_pct": {
   "min": 88.0,
   "max": 92.0,
   "mean": 90.94117647058823
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1935.0,
   "mean": 1934.4117647058824
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "6b96a01278dd8a65faed5e90d8ae1bc19a89cca132f7d1e87f8f5c652b4ffbaf",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOG.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOG.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n           .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:413). The PLE table lives on\nNVMe and is served through mmap, so `PLE` must point at a file on NVMe. The server's own log is written to\n`$LOGDIR` (default `~/quant/logs/`); `--enable-ple` writes `ple/ple.json` into `$LOGDIR` at startup.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-control patches, the Triton configs\nkernels/        the Triton kernels: int2 MoE gemm, int8 PLE gemm, the PLE gather, the int4 KV pool, the int8 ring\nscripts/        serve.sh (the launcher), the build and validation scripts, the elastic-control daemon\ntools/        the measurement tools: bench.py, ple.py, ngram.py, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh,\n            ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe.sh, ngram_probe.py, ngram_probe",
 "ttft_seconds": 0.440373425008147,
 "status": "ok",
 "tg_tokens_per_s": 37.153016223937755,
 "decode_seconds_per_token": 0.026915715105674202,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.763745001517236,
  "p95": 33.296380992396735,
  "mean": 26.915715105674202
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
