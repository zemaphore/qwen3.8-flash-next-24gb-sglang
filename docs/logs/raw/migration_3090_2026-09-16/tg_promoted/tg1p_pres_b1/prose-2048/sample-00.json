{
 "started_utc": "2026-09-17T03:54:59.008315+00:00",
 "wall_seconds": 17.07708570601244,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:55:16.085619+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 61,
  "temperature_c": {
   "min": 60.0,
   "max": 69.0,
   "mean": 65.55737704918033
  },
  "power_w": {
   "min": 123.32,
   "max": 272.17,
   "mean": 235.23081967213113
  },
  "util_pct": {
   "min": 0.0,
   "max": 95.0,
   "mean": 80.50819672131148
  },
  "clock_sm_mhz": {
   "min": 1740.0,
   "max": 1950.0,
   "mean": 1929.5901639344263
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "5034b5dead2a51467eab1be40bc3dc5489ee6f183e9a1e36f435ef3b88a0728d",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/00.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch     .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-K.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/00.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/0002.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/0002-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/0002-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/0002-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`, `accepted.decode_all`); the patch's flags are in\n`sglang/qwen4exp-serving-73a255206f.patch` (CAMPAIGN.md:320).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file, applied to SGLang at commit 73a255206f)\npatches/        the layered patch scripts the patch was built from (10 files)\nkernels/        the Triton kernels (4 files)\nassets/         the two serving assets: the PLE n-gram table and the expert cache (2 files)\ntools/          the measurement tools (5 files)\ndocs/           the design documents and the engineering log (10 files)\nscripts/        the launcher and the build scripts (5 files)\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single patch that was applied to SGLang at commit\n`73a255206f` and measured. It is the union of the layers in `patches/`, applied in the order below; the\npatch file is the artifact, the layers are how it was built.\n\n```\npatches/0001-ple-architecture.patch      the Qwen4-Exp architecture: PLE, the shared expert, the\n                                         16-bit router, the 128-expert MoE, the 128-head Q/128-KV-head K\n                                         with a 128-token sliding window, the 128-slot ring attention\npatches/0002-ple-ple",
 "ttft_seconds": 3.092154746002052,
 "status": "ok",
 "tg_tokens_per_s": 36.539633820443065,
 "decode_seconds_per_token": 0.027367543005877733,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.989040998159908,
  "p95": 35.318327994900756,
  "mean": 27.367543005877735
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
