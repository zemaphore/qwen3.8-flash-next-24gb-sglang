{
 "started_utc": "2026-09-17T03:55:16.087884+00:00",
 "wall_seconds": 14.806611403007992,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:55:30.894834+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 67.0,
   "max": 70.0,
   "mean": 68.75471698113208
  },
  "power_w": {
   "min": 250.71,
   "max": 270.05,
   "mean": 261.16037735849056
  },
  "util_pct": {
   "min": 86.0,
   "max": 98.0,
   "mean": 89.81132075471699
  },
  "clock_sm_mhz": {
   "min": 1740.0,
   "max": 1935.0,
   "mean": 1921.9811320754718
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "c9b54048e3b59fa7a354b91dfd05c7418fc75d79c04e070eb945883aed832cf6",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,11.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4_int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4_int8_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4_int8_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4_int8_int4_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4_int8_int4_bf.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted checkpoint is `checkpoint-10000` (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files, 1,110 lines)\nkernels/        the Triton kernels (int2 MoE, int8 PLE, int4 KV pool, int8 ring, int8 rope, int8 n-gram)\nassets/         the two serving assets: the PLE n-gram table and the accepted state\ntools/          the measurement tools (bench, ngram, nll, ngram_nll, ngram_nll_long, ngram_nll_258k,\n                ngram_nll_258k_bf16, ngram_nll_258k_int4, ngram_nll_258k_int8ring, ngram_nll_258k_int8ring_int4,\n                ngram_nll_258k_int8ring_int4_bf16, ngram_nll_258k_int8ring_int4_int4, ngram_nll_258k_int8ring_int4_int8,\n                ngram_nll_258k_int8ring_int4_int8_bf16, ngram_nll_258k_int8ring_int4_int8_int4,\n                ngram_nll_258k_int8ring_int4_int8_int4_bf1",
 "ttft_seconds": 1.377987761006807,
 "status": "ok",
 "tg_tokens_per_s": 38.05341226108612,
 "decode_seconds_per_token": 0.026278852291588366,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.029454995295964,
  "p95": 32.31696599686984,
  "mean": 26.278852291588365
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
