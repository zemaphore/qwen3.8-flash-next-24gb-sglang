{
 "started_utc": "2026-09-17T04:07:14.014128+00:00",
 "wall_seconds": 17.74896596999315,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:07:31.763334+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 63,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.7936507936508
  },
  "power_w": {
   "min": 231.18,
   "max": 254.94,
   "mean": 247.7785714285714
  },
  "util_pct": {
   "min": 90.0,
   "max": 95.0,
   "mean": 92.52380952380952
  },
  "clock_sm_mhz": {
   "min": 1920.0,
   "max": 1920.0,
   "mean": 1920.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 290.0
  }
 },
 "output_text_sha256": "e07a8c479bcc6901edc262907aaefedcfe745187be75de7a58aa37e197927500",
 "output_text": "_int_int4_int4`_int4` :_int4` :9_int4` :91_int4` :914_int4` :914-_int4` :914-9_int4` :914-95_int4` :914-952_int4` :914-952,_int4` :914-952, before_int4` :914-952, before `_int4` :914-952, before `q_int4` :914-952, before `qwen_int4` :914-952, before `qwen_sparse_int4` :914-952, before `qwen_sparse_valid_int4` :914-952, before `qwen_sparse_valid_counts_int4` :914-952, before `qwen_sparse_valid_counts_tr_int4` :914-952, before `qwen_sparse_valid_counts_triton_int4` :914-952, before `qwen_sparse_valid_counts_triton`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :_int4` :914-952, before `qwen_sparse_valid_counts_triton` :9_int4` :914-952, before `qwen_sparse_valid_counts_triton` :95_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch;_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr   _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots &_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & R_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                        _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         #_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots %_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % R_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no   _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner +_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1)._int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(t_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int6_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid &_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o ==_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                      _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       #_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot ->_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid &_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o !=_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                      _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       #_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # ->_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\n_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `__int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_t_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tier_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `__int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:4_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:45_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-5_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-50_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/s_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot &_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & R_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `s_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`),_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus**_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp1_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..._int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(...,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 6_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 655_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 6550_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504._int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:4_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:48_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:4_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:49_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :7_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :72_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728;_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAM_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPA_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:3_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:39_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392),_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `__int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:6_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:68_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-7_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-75_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) ver_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x _int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 5_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 51_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant),_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner +_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(t_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int3_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value;_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign)._int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_t_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tier_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(..._int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:5_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:50_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-5_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-53_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :7_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :75_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-7_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-78_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape ==_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `r_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape ==_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//6_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.num_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel()_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() ==_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK =_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`._int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H)_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) ->_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer`_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward,_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :17_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :172_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-17_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-172_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728)._int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf1_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option:_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and `_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and `tl_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and `tl.store_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and `tl.store(r_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and `tl.store(rk_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and `tl.store(rk_b_int4` :914-952, before `qwen_sparse_valid_counts_triton` :954 \u2014 the same anchor kv_int4.py used)\n\nCommon device-side tier test used by every reader (no host branch; owner is a static tensor read at replay):\n```\nr    = slots & RING_MASK                         # (or slots % RING if MOD)\no    = tl.load(owner + r, mask=valid, other=-1).to(tl.int64)\nhot  = valid & (o == slots)                       # ring row belongs to this slot -> int8\ncold = valid & (o != slots)                       # -> int4 row (always exists under dual-write)\n```\n\na) Write: `_quant_store_kv_tiered[(N, H)]` = body of `_quant_store_kv_int8` (:455-503) with row/srow computed from `r = slot & RING_MASK` (`row8 = (r*H+h)*D`, `srow8 = (r*H+h)*NG8`), **plus** the fp16 clamp `s = tl.minimum(..., 65504.0)` the int8 kernel lacks (:488/:498 vs :728; CAMPAIGN.md:392), followed by the body of `_quant_store_kv_int4` (:684-753) verbatim at `row4 = (slot*H+h)*DH` (loads of k/v are re-issued even/odd from L1/L2 \u2014 3 x 512 B per head, irrelevant), then `tl.store(owner + r, slot.to(tl.int32))` (both heads store the same value; benign). Wrapper `quant_store_kv_tiered(...)` = the asserts of both wrappers (:505-537, :755-789) plus `rk.shape == (R, H, D)`, `rks.shape == (R, H, D//64)`, `owner.numel() == R`, `RING_MASK = R-1`. Static grid (N, H) -> capture-safe like today (`set_kv_buffer` runs inside the captured forward, backend :1726-1728). bf16 ring option: skip the int8 half and `tl.store(rk_bf",
 "ttft_seconds": 0.5180625999928452,
 "status": "ok",
 "tg_tokens_per_s": 29.65615454478184,
 "decode_seconds_per_token": 0.033719813487280176,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.3378790091956,
  "p95": 40.98668800725136,
  "mean": 33.71981348728018
 },
 "class": "reasoning",
 "requested_input_tokens": 131072
}
