{
 "started_utc": "2026-09-17T04:10:45.451202+00:00",
 "wall_seconds": 18.113782401997014,
 "prompt_tokens": 131072,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:11:03.565064+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 64,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 76.28125
  },
  "power_w": {
   "min": 227.96,
   "max": 275.78,
   "mean": 247.7921875
  },
  "util_pct": {
   "min": 87.0,
   "max": 96.0,
   "mean": 91.71875
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1919.0625
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 108.0
  }
 },
 "output_text_sha256": "74353930d5e5dc5c255494fdcff4563c3ff6690f915d7dea534facb9c4a145dc",
 "output_text": "\"\"   \"    LOG\"    LOGPRO\"    LOGPROB\"    LOGPROB_MAX\"    LOGPROB_MAX={\"    LOGPROB_MAX={overall\"    LOGPROB_MAX={overall['\"    LOGPROB_MAX={overall['max\"    LOGPROB_MAX={overall['max']\"    LOGPROB_MAX={overall['max']:.\"    LOGPROB_MAX={overall['max']:.6\"    LOGPROB_MAX={overall['max']:.6f\"    LOGPROB_MAX={overall['max']:.6f}\"    LOGPROB_MAX={overall['max']:.6f} LOG\"    LOGPROB_MAX={overall['max']:.6f} LOGPRO\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_ME\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() ->\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) <\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n           \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\" \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                 \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced log\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logpro\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(c\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2],\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D /\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report =\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b}\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\",\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n       \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ ==\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n   \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode G\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEM\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4])\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) -\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B:\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-cont\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16,\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N])\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) -\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n \"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gem\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_band\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport tr\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sg\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.s\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.m\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.exp\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gem\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _m\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gem\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sg\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.s\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.m\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.m\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wn\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna1\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _m\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gem\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gemv\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gemv_int\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gemv_int2\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gemv_int2\n\n\n\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gemv_int2\n\n\ndef\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gemv_int2\n\n\ndef bench\"    LOGPROB_MAX={overall['max']:.6f} LOGPROB_MEAN={overall['mean']:.6f}\")\n\n\ndef main() -> None:\n    if len(sys.argv) < 2:\n        print(__doc__)\n        sys.exit(1)\n    cmd = sys.argv[1]\n    if cmd == \"save\":\n        name = sys.argv[2]\n        payload = collect()\n        (D / f\"{name}.json\").write_text(json.dumps(payload))\n        for key, row in payload.items():\n            print(f\"  {key}: prompt {row['prompt_tokens']} tokens, \"\n                  f\"{row['forced_tokens']} forced logprobs saved\")\n        return\n    if cmd == \"check\":\n        name = sys.argv[2]\n        reference = json.loads((D / f\"{name}.json\").read_text())\n        current = collect(cached=reference)\n        report = stats(reference, current)\n        print_report(f\"{name} vs current\", report)\n        return\n    if cmd == \"compare\":\n        a, b = sys.argv[2], sys.argv[3]\n        ra = json.loads((D / f\"{a}.json\").read_text())\n        rb = json.loads((D / f\"{b}.json\").read_text())\n        report = stats(ra, rb)\n        print_report(f\"{b} vs {a}\", report)\n        return\n    sys.exit(f\"unknown command {cmd!r}\")\n\n\nif __name__ == \"__main__\":\n    main()\n\n\n#!/usr/bin/env python3\n\"\"\"Measure the decode GEMV's read bandwidth, without loading the model.\n\nTwo variants of the same idea, measured on the same tensors:\n  A: byte-row layout (uint8 [E, N, K/4]) - the current loader layout\n  B: N-contiguous int32 words (int32 [E, K/16, N]) - the proposed layout\n\n  python3 gemv_bandwidth.py [E] [K] [N]\n\"\"\"\nimport sys\nimport torch\nimport triton\n\nfrom sglang.srt.layers.moe.expert_gemv import _moe_gemv_int2_tab\nfrom sglang.srt.layers.moe.moe_wna16_int2 import _moe_gemv_int2\n\n\ndef bench(fn",
 "ttft_seconds": 0.492809456991381,
 "status": "ok",
 "tg_tokens_per_s": 28.999623036377898,
 "decode_seconds_per_token": 0.034483206859122734,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 34.50248099397868,
  "p95": 45.7170969893923,
  "mean": 34.48320685912273
 },
 "class": "code",
 "requested_input_tokens": 131072
}
