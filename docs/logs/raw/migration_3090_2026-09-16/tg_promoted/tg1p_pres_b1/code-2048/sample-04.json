{
 "started_utc": "2026-09-17T03:58:53.042316+00:00",
 "wall_seconds": 15.117675533998408,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T03:59:08.160048+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 54,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.66666666666667
  },
  "power_w": {
   "min": 247.85,
   "max": 267.6,
   "mean": 258.1816666666667
  },
  "util_pct": {
   "min": 87.0,
   "max": 94.0,
   "mean": 90.92592592592592
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1918.888888888889
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "5bec92ef7de8bb7e55f2f73657e62fbf4563f1844c0459c5645101b538346df8",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 12+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 12+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # ------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.c+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M),+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # --------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, Z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and S+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        # boundary+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        # boundary (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        # boundary (BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        # boundary (BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        # boundary (BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        # boundary (BLOCK_SIZE_K %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if not has_zp and use_int4_w4a16:\n+        b_zp_num = 8\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+\n+    if not has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = offs_bn[None, :] % 2\n+\n+    # -----------------------------------------------------------\n+    # Iterate to compute a block of the C matrix.\n+    # We accumulate into a `[BLOCK_SIZE_M, BLOCK_SIZE_N]` block\n+    # of fp32 values for higher accuracy.\n+    # `accumulator` will be converted back to fp16 after the loop.\n+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)\n+    for k in range(0, tl.cdiv(K, BLOCK_SIZE_K)):\n+        # Load the next block of A and B,  generate a mask based on size\n+        # of token ID (M), and K (N) respectively.\n+        if not even_Ks:\n+            k_mask = offs_k[:, None] < K\n+            k_mask = k_mask & (offs_token < num_valid_tokens)\n+            k_mask_a = k_mask & (offs_token[:, None] // top_k < num_valid_tokens)\n+            k_mask_b = k_mask & (offs_bn[None, :] % N)\n+\n+        # -----------------------------------\n+        # Load A, B, ZPs and Scales\n+        # Note that for int2 word loads, the K mask is handled by the word\n+        # boundary (BLOCK_SIZE_K % ",
 "ttft_seconds": 0.3311649840034079,
 "status": "ok",
 "tg_tokens_per_s": 34.55864963920012,
 "decode_seconds_per_token": 0.02893631581211127,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 28.64095200493466,
  "p95": 37.714263991802,
  "mean": 28.93631581211127
 },
 "class": "code",
 "requested_input_tokens": 2048
}
